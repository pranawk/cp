#include<bits/stdc++.h>
using namespace std;

void solve(nums,){
    priority_queue<int>pq;
    for(int j=ii; j<ii+m; j++)pq.push(nums[j]);
    for(int i=ii+m, i<n-(m*k); i++){
        sol

    }
}

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int n,m,k;
    cin>>n>>m>>k;
    vector<int>nums(n);
    for(int i=0; i<n; i++)cin>>nums[i];

    return 0;
}
