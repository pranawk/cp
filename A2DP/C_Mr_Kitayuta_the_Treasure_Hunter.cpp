//  C. Mr. Kitayuta, the Treasure Hunter

#include<bits/stdc++.h>

using namespace std;
int dp[30002];
int solve(vector<int>&a, int i, int d){
    if(i>a[a.size()-1])return 0;
    if(st.find(a[i])!=st.end()){
        if(dp[])
    }
}
int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int n,d;
    cin>>n>>d;
    vector<int>a(n);
    for(int i=0; i<n; i++)cin>>a[i];
    for(int i=0; i<3002; i++)dp[i]=-1;
    return 0;
}